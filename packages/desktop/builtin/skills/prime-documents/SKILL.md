---
name: prime-documents
description: Create or edit Word DOCX reports, letters, proposals and structured documents with artifact-toolkit.
---

# 문서 작성

1. Read the supplied content/template and identify audience, required sections, page size, typography and editable output format.
2. Load prime-artifacts for the managed Python toolkit and inspect the original DOCX before edits. Use python-docx for paragraphs, styles, tables, headings, headers/footers and page breaks.
3. Preserve original content and intentional formatting. Use a small coherent style system, real heading styles and consistent table widths. Do not approximate a requested existing template without inspecting it.
4. Save a separate output DOCX and reopen it. Verify all required text/tables and document structure. Render through an available office renderer or PDF exporter and inspect pages when available; disclose when only structural validation was possible.
5. Link the editable file and any verified preview. Never claim tracked changes or legal review unless actually implemented/performed.
